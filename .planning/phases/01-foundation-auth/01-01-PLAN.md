---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
  - .env.example
  - client/package.json
  - client/tsconfig.json
  - client/tsconfig.app.json
  - client/tsconfig.node.json
  - client/vite.config.ts
  - client/index.html
  - client/.env
  - client/.env.example
  - client/src/main.tsx
  - client/src/App.tsx
  - client/src/index.css
  - client/src/vite-env.d.ts
  - server/package.json
  - server/tsconfig.json
  - server/nodemon.json
  - server/.env
  - server/.env.example
  - server/src/server.ts
  - server/src/app.ts
  - server/src/config/env.ts
  - server/src/config/db.ts
  - server/src/config/logger.ts
  - server/src/middleware/error-handler.ts
  - server/src/middleware/not-found.ts
  - server/src/types/index.ts
autonomous: true
requirements:
  - SECR-04
user_setup: []
must_haves:
  truths:
    - Monorepo with client/ and server/ directories
    - TypeScript configured in both projects
    - Root package.json uses concurrently to run both dev servers
    - Server connects to MongoDB via Mongoose
    - OpenAI API key only in server/.env, not in client/.env
    - Vite proxy forwards /api to Express server
  artifacts:
    - package.json (root with concurrently scripts)
    - client/ (Vite + React 19 + TypeScript project)
    - server/ (Express + TypeScript project with MongoDB connection)
    - .gitignore (covers node_modules, .env, dist for both projects)
  key_links: []
---

<objective>
Scaffold the complete monorepo project structure with Vite+React+TypeScript client and Express+TypeScript server, MongoDB connection, root package.json with concurrently, environment configuration, and base error handling.

Purpose: Establishes the foundation that all subsequent plans depend on — working dev servers, database connection, and project structure.
Output: Fully scaffolded monorepo with both client and server runnable via `npm run dev` from root.
</objective>

<execution_context>
@C:/Users/mobin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/mobin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create root package.json, .gitignore, .env.example, and scaffold client with Vite+React+TS</name>
  <files>package.json, .gitignore, .env.example, client/package.json, client/tsconfig.json, client/tsconfig.app.json, client/tsconfig.node.json, client/vite.config.ts, client/index.html, client/.env, client/.env.example, client/src/main.tsx, client/src/App.tsx, client/src/index.css, client/src/vite-env.d.ts</files>
  <read_first>.planning/phases/01-foundation-auth/01-RESEARCH.md</read_first>
  <action>
1. Create root `package.json`:
```json
{
  "name": "studyai",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,green \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build:client": "npm run build --prefix client",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
```

2. Run `npm install` in root to install concurrently.

3. Create `.gitignore` at root with entries for:
```
node_modules/
dist/
.env
.env.local
*.log
.DS_Store
Thumbs.db
```

4. Create `.env.example` at root documenting all env vars for both client and server (reference only, actual .env files in subdirectories).

5. Run `npm create vite@latest client -- --template react-ts` to scaffold the Vite+React+TS client project. Then `cd client && npm install`.

6. Install client dependencies (run from client/ directory):
```bash
npm install react-router-dom@^7.3 zustand@^5.0 @tanstack/react-query@^5.67 zod@^3.24 react-hook-form@^7.54 @hookform/resolvers@^4.1 axios@^1.7 lucide-react@^0.475 clsx@^2.1 tailwind-merge@^3.0 sonner@^2.0 class-variance-authority@^0.7.1
```

7. Install client dev dependencies:
```bash
npm install -D tailwindcss@^4.0 @tailwindcss/vite@^4.0 vite-tsconfig-paths
```

8. Update `client/vite.config.ts`:
```typescript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";
import tsconfigPaths from "vite-tsconfig-paths";

export default defineConfig({
  plugins: [react(), tailwindcss(), tsconfigPaths()],
  server: {
    port: 5173,
    proxy: {
      "/api": {
        target: "http://localhost:3000",
        changeOrigin: true,
      },
    },
  },
});
```

9. Update `client/tsconfig.app.json` to add path alias:
```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```
(Merge with existing compilerOptions from Vite template)

10. Replace `client/src/index.css` with:
```css
@import "tailwindcss";
```

11. Update `client/src/main.tsx` to apply dark theme by default:
```typescript
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import App from "./App.tsx";
import "./index.css";

const theme = localStorage.getItem("theme") || "dark";
document.documentElement.classList.toggle("dark", theme === "dark");

createRoot(document.getElementById("root")!).render(
  <StrictMode>
    <App />
  </StrictMode>
);
```

12. Update `client/src/App.tsx` to a minimal placeholder:
```typescript
function App() {
  return (
    <div className="min-h-screen bg-background text-foreground">
      <h1 className="text-2xl font-bold p-8">StudyAI</h1>
    </div>
  );
}

export default App;
```

13. Create `client/.env`:
```
VITE_API_URL=http://localhost:3000
```

14. Create `client/.env.example`:
```
VITE_API_URL=http://localhost:3000
```
  </action>
  <verify>
- Run `npm run dev:client` from root and confirm Vite dev server starts on port 5173
- Check that `client/package.json` contains `react`, `react-dom`, `zustand`, `@tanstack/react-query`, `zod`, `axios` as dependencies
- Check that `client/vite.config.ts` includes tailwindcss and tsconfigPaths plugins
- Check that `client/src/index.css` contains `@import "tailwindcss"`
  </verify>
  <acceptance_criteria>
    - `package.json` at root contains `"concurrently"` in devDependencies
    - `package.json` at root contains script `"dev"` with `concurrently`
    - `client/package.json` contains `"react": "^19` in dependencies
    - `client/package.json` contains `"zustand"` in dependencies
    - `client/package.json` contains `"@tanstack/react-query"` in dependencies
    - `client/package.json` contains `"zod"` in dependencies
    - `client/package.json` contains `"axios"` in dependencies
    - `client/package.json` contains `"tailwindcss"` in devDependencies
    - `client/package.json` contains `"@tailwindcss/vite"` in devDependencies
    - `client/vite.config.ts` contains `tailwindcss()` and `tsconfigPaths()`
    - `client/vite.config.ts` contains `proxy` with `"/api"` targeting `http://localhost:3000`
    - `client/src/index.css` contains `@import "tailwindcss"`
    - `client/src/main.tsx` contains `localStorage.getItem("theme")`
    - `.gitignore` contains `node_modules/` and `.env`
    - `client/.env` contains `VITE_API_URL`
    - No file in `client/.env` or `client/.env.example` contains `OPENAI`
  </acceptance_criteria>
  <done>Root package.json with concurrently installed, client/ scaffolded with Vite+React 19+TS, all client deps installed, Tailwind v4 configured, Vite proxy configured, dark theme default applied in main.tsx</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Express+TypeScript server with MongoDB connection, env validation, logger, and base middleware</name>
  <files>server/package.json, server/tsconfig.json, server/nodemon.json, server/.env, server/.env.example, server/src/server.ts, server/src/app.ts, server/src/config/env.ts, server/src/config/db.ts, server/src/config/logger.ts, server/src/middleware/error-handler.ts, server/src/middleware/not-found.ts, server/src/types/index.ts</files>
  <read_first>.planning/phases/01-foundation-auth/01-RESEARCH.md</read_first>
  <action>
1. Create `server/` directory structure:
```
server/
├── src/
│   ├── config/
│   ├── middleware/
│   ├── models/
│   ├── routes/
│   ├── services/
│   ├── schemas/
│   └── types/
```

2. Create `server/package.json`:
```json
{
  "name": "studyai-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nodemon",
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "mongoose": "^8.12.0",
    "jsonwebtoken": "^9.0.0",
    "bcryptjs": "^2.4.0",
    "cookie-parser": "^1.4.0",
    "cors": "^2.8.0",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0",
    "compression": "^1.7.0",
    "express-rate-limit": "^7.5.0",
    "zod": "^3.24.0",
    "dotenv": "^16.4.0",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "nodemon": "^3.1.0",
    "@types/express": "latest",
    "@types/cors": "latest",
    "@types/morgan": "latest",
    "@types/compression": "latest",
    "@types/cookie-parser": "latest",
    "@types/jsonwebtoken": "latest",
    "@types/bcryptjs": "latest"
  }
}
```

3. Run `npm install` in `server/` directory.

4. Create `server/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "commonjs",
    "lib": ["ES2022"],
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

5. Create `server/nodemon.json`:
```json
{
  "watch": ["src"],
  "ext": "ts",
  "exec": "tsx src/server.ts"
}
```

6. Create `server/.env`:
```
PORT=3000
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/studyai
JWT_ACCESS_SECRET=dev-access-secret-change-in-production-min32chars
JWT_REFRESH_SECRET=dev-refresh-secret-change-in-production-min32chars
CLIENT_URL=http://localhost:5173
OPENAI_API_KEY=sk-placeholder-add-in-phase-2
```

7. Create `server/.env.example`:
```
PORT=3000
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/studyai
JWT_ACCESS_SECRET=your-access-secret-min-32-chars-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-min-32-chars-change-in-production
CLIENT_URL=http://localhost:5173
OPENAI_API_KEY=sk-your-openai-api-key
```

8. Create `server/src/config/env.ts` — Zod-validated environment variables:
```typescript
import { z } from "zod";
import dotenv from "dotenv";

dotenv.config();

const envSchema = z.object({
  PORT: z.string().default("3000"),
  NODE_ENV: z.enum(["development", "production", "test"]).default("development"),
  MONGODB_URI: z.string().min(1, "MONGODB_URI is required"),
  JWT_ACCESS_SECRET: z.string().min(32, "JWT_ACCESS_SECRET must be at least 32 characters"),
  JWT_REFRESH_SECRET: z.string().min(32, "JWT_REFRESH_SECRET must be at least 32 characters"),
  CLIENT_URL: z.string().default("http://localhost:5173"),
});

export const env = envSchema.parse(process.env);
```

9. Create `server/src/config/db.ts` — Mongoose connection:
```typescript
import mongoose from "mongoose";
import { env } from "./env";
import { logger } from "./logger";

export async function connectDB(): Promise<void> {
  try {
    await mongoose.connect(env.MONGODB_URI);
    logger.info("Connected to MongoDB");
  } catch (error) {
    logger.error("MongoDB connection error:", error);
    process.exit(1);
  }

  mongoose.connection.on("error", (error) => {
    logger.error("MongoDB error:", error);
  });

  mongoose.connection.on("disconnected", () => {
    logger.warn("MongoDB disconnected");
  });
}
```

10. Create `server/src/config/logger.ts` — Winston logger:
```typescript
import winston from "winston";

export const logger = winston.createLogger({
  level: process.env.NODE_ENV === "production" ? "info" : "debug",
  format: winston.format.combine(
    winston.format.timestamp(),
    winston.format.errors({ stack: true }),
    winston.format.json()
  ),
  transports: [
    new winston.transports.Console({
      format: winston.format.combine(
        winston.format.colorize(),
        winston.format.simple()
      ),
    }),
  ],
});
```

11. Create `server/src/types/index.ts` — shared server types including ApiResponse envelope and AuthRequest:
```typescript
import { Request } from "express";

export interface AuthRequest extends Request {
  userId?: string;
  userEmail?: string;
  userLevel?: string;
}

export interface ApiSuccessResponse<T> {
  success: true;
  data: T;
}

export interface ApiErrorResponse {
  success: false;
  error: {
    code: string;
    message: string;
    details?: Array<{ field: string; message: string }>;
  };
}

export type ApiResponse<T> = ApiSuccessResponse<T> | ApiErrorResponse;
```

12. Create `server/src/middleware/error-handler.ts` with AppError class and global error handler:
```typescript
import { Request, Response, NextFunction } from "express";
import { logger } from "../config/logger";

export class AppError extends Error {
  statusCode: number;
  code: string;
  isOperational: boolean;

  constructor(message: string, statusCode: number, code: string) {
    super(message);
    this.statusCode = statusCode;
    this.code = code;
    this.isOperational = true;
  }
}

export function errorHandler(
  err: Error,
  _req: Request,
  res: Response,
  _next: NextFunction
): void {
  if (err instanceof AppError) {
    res.status(err.statusCode).json({
      success: false,
      error: { code: err.code, message: err.message },
    });
    return;
  }

  logger.error("Unhandled error:", err);
  res.status(500).json({
    success: false,
    error: { code: "INTERNAL_ERROR", message: "An unexpected error occurred" },
  });
}
```

13. Create `server/src/middleware/not-found.ts`:
```typescript
import { Request, Response } from "express";

export function notFoundHandler(_req: Request, res: Response): void {
  res.status(404).json({
    success: false,
    error: { code: "NOT_FOUND", message: "The requested resource was not found" },
  });
}
```

14. Create `server/src/app.ts` — Express app with global middleware stack (helmet, cors, compression, morgan, json, cookieParser):
```typescript
import express from "express";
import helmet from "helmet";
import cors from "cors";
import compression from "compression";
import morgan from "morgan";
import cookieParser from "cookie-parser";
import { env } from "./config/env";
import { errorHandler } from "./middleware/error-handler";
import { notFoundHandler } from "./middleware/not-found";

const app = express();

app.use(helmet());
app.use(cors({
  origin: env.CLIENT_URL,
  credentials: true,
  methods: ["GET", "POST", "PUT", "PATCH", "DELETE"],
  allowedHeaders: ["Content-Type", "Authorization"],
}));
app.use(compression());
app.use(morgan(env.NODE_ENV === "production" ? "combined" : "dev"));
app.use(express.json({ limit: "1mb" }));
app.use(express.urlencoded({ extended: false }));
app.use(cookieParser());

// Health check
app.get("/api/health", (_req, res) => {
  res.json({ success: true, data: { status: "ok", timestamp: new Date().toISOString() } });
});

// 404 handler
app.use(notFoundHandler);

// Global error handler
app.use(errorHandler);

export default app;
```

15. Create `server/src/server.ts` — entry point that connects DB then starts listening:
```typescript
import app from "./app";
import { env } from "./config/env";
import { connectDB } from "./config/db";
import { logger } from "./config/logger";

async function start(): Promise<void> {
  await connectDB();

  app.listen(Number(env.PORT), () => {
    logger.info(`Server running on port ${env.PORT} in ${env.NODE_ENV} mode`);
  });
}

start().catch((error) => {
  logger.error("Failed to start server:", error);
  process.exit(1);
});
```
  </action>
  <verify>
- Run `npx tsc --noEmit` in server/ to verify TypeScript compiles without errors
- Check `server/src/config/env.ts` contains `envSchema` with `MONGODB_URI`, `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`
- Check `server/src/app.ts` imports and uses `helmet`, `cors`, `compression`, `morgan`, `cookieParser`
- Check `server/.env` contains `OPENAI_API_KEY` and `server/.env` does NOT contain any `VITE_` prefix variables
- Check no file in `client/.env` or `client/.env.example` contains `OPENAI`
  </verify>
  <acceptance_criteria>
    - `server/package.json` contains `"express": "^4.21` in dependencies
    - `server/package.json` contains `"mongoose"` in dependencies
    - `server/package.json` contains `"jsonwebtoken"` in dependencies
    - `server/package.json` contains `"bcryptjs"` in dependencies
    - `server/package.json` contains `"zod"` in dependencies
    - `server/package.json` contains `"tsx"` in devDependencies
    - `server/tsconfig.json` contains `"strict": true`
    - `server/nodemon.json` contains `"exec": "tsx src/server.ts"`
    - `server/src/config/env.ts` contains `envSchema` and `z.object`
    - `server/src/config/env.ts` contains `JWT_ACCESS_SECRET: z.string().min(32`
    - `server/src/config/db.ts` contains `mongoose.connect`
    - `server/src/config/logger.ts` contains `winston.createLogger`
    - `server/src/app.ts` contains `app.use(helmet())`
    - `server/src/app.ts` contains `credentials: true`
    - `server/src/app.ts` contains `"/api/health"`
    - `server/src/app.ts` contains `app.use(errorHandler)`
    - `server/src/middleware/error-handler.ts` contains `class AppError`
    - `server/src/middleware/not-found.ts` contains `NOT_FOUND`
    - `server/src/types/index.ts` contains `AuthRequest`
    - `server/src/types/index.ts` contains `ApiSuccessResponse`
    - `server/.env` contains `OPENAI_API_KEY`
    - `server/.env` does NOT contain `VITE_`
  </acceptance_criteria>
  <done>Server scaffolded with Express+TS, MongoDB connection via Mongoose, Zod env validation, Winston logger, helmet/cors/compression/morgan/cookieParser middleware, health endpoint, error handler, and 404 handler. TypeScript compiles cleanly.</done>
</task>
</tasks>

<verification>
- [ ] `npm run dev:client` starts Vite on port 5173 (stop after confirming startup)
- [ ] `npx tsc --noEmit` in server/ passes with no errors
- [ ] `server/.env` contains `OPENAI_API_KEY` and no `VITE_` vars
- [ ] `client/.env` contains NO `OPENAI` references (SECR-04)
- [ ] `.gitignore` includes `node_modules/`, `.env`, `dist/`
- [ ] Health check endpoint exists at `/api/health` in app.ts
</verification>

<success_criteria>
- All tasks completed
- Both client and server projects scaffold cleanly
- TypeScript compiles in both projects
- SECR-04 verified: no OpenAI keys in client env files
- Root package.json runs both projects via concurrently
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
